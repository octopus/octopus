<verification>

<!-- ******************************************************************************** -->
<!-- predefined vars:
     $subdir      -> execution dir of benchmark run
     $stdoutfile  -> $stdout file of bnechmark run
     $stderrfile  -> $stderr file of bnechmark run
     $...         -> params from benchmark specification in toplevel dir     
-->

<verify cname="c240">
  <command>
    perl ./verify-c240.pl $subdir/verify.xml $subdir
  </command>
</verify>

<verify cname="chlorophyll">
  <command>
    perl ./verify-chlorophyll.pl $subdir/verify.xml $subdir
  </command>
</verify>

</verification>
